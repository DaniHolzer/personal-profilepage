import { tv } from "tailwind-variants";

//-------------------------------------------------------------------------------------------------
// Animated Menu Button Styling

export const lineStyling = tv({
  base: [
    "absolute left-[5px] h-[2px] w-[20px] bg-blue-500 transition-transform duration-500"
  ],
})

