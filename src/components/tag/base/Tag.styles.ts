import { tv } from "tailwind-variants";

//-----------------------------------------------------------------------------
// Tag Styles

export const tagStyles = tv({
  base: "py-[0.5rem] px-[1rem] text-[0.8rem] rounded-full text-bgPrimary",
})
