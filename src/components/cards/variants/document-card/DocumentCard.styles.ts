import { tv } from "tailwind-variants";

//-----------------------------------------------------------------------------
// Document Card Styles

export const documentCardStyles = tv({
  base: "items-center"
})
