import { tv } from 'tailwind-variants';

//-----------------------------------------------------------------------------
// Layout - Styles

export const layoutStyles = tv({
  base: 'bg-red-600 flex flex-col justify-start items-center mx-auto my-0 min-h-screen',
});
